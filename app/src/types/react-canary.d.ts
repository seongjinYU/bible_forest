// View Transitions API (Next 16 experimental.viewTransition).
// `<ViewTransition>` 와 `addTransitionType` 타입은 react canary 채널에 선언돼 있어
// 프로젝트에서 한 번 참조해 활성화한다. (런타임은 Next 번들 React가 제공)
/// <reference types="react/canary" />

export {};
